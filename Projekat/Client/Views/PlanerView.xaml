<Window x:Class="Client.Views.PlanerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Views"
        xmlns:cal="http://caliburnmicro.com"
        mc:Ignorable="d"
        Title="PlanerView" Height="450" Width="800">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="boolToVis" />
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="30"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Column="1" Grid.Row="0" Content="Undo" x:Name="UndoCommand" IsEnabled="{Binding}"/>
        <Button Grid.Column="2" Grid.Row="0" Content="Redo" x:Name="RedoCommand" IsEnabled="{Binding}"/>
        <Button Grid.Column="4" Grid.Row="0" Content="Napravi Plan" x:Name="CreatePlan"/>

        <Button Grid.Column="5" Grid.Row="0" Content="Dodaj Novog Korisnika" x:Name="AddNewUser" Visibility="{Binding IsAdmin, Converter={StaticResource boolToVis}}"/>

        <Button Grid.Column="6" Grid.Row="0" Content="Promeni Svoje Podatke" x:Name="ChangeUserData"/>

        <Button Grid.Column="7" Grid.Row="0" Content="Logout" x:Name="Logout" />

        <DataGrid x:Name="PlanerModel_Planers" Grid.Column="1" Grid.ColumnSpan="7"  Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" SelectedItem="{Binding Path=SelectedPlaner, Mode=OneWayToSource}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Naziv" Binding="{Binding Path=Naziv, Mode=OneWay}"/>
                <DataGridTextColumn Header="Opis" Binding="{Binding Path=Opis, Mode=OneWay}" Width="*"/>
                <DataGridTextColumn Header="Datum pocetka" Binding="{Binding Path=DatumPocetka, Mode=OneWay, StringFormat=d}"/>
                <DataGridTextColumn Header="Datum zavrsetka" Binding="{Binding Path=DatumZavrsetka, Mode=OneWay, StringFormat=d}"/>
                <DataGridTextColumn Header="Kreator Plana" Binding="{Binding Path=PlanerCreator.FullName, Mode=OneWay}" />
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" x:Name="EditPlaner" cal:Bind.Model="{Binding DataContext, RelativeSource={RelativeSource AncestorType=DataGrid}}" cal:Message.Attach="EditButton" />
                                <Button Content="Duplicate" Margin="5 0" x:Name="DuplicatePlaner" cal:Bind.Model="{Binding DataContext, RelativeSource={RelativeSource AncestorType=DataGrid}}" cal:Message.Attach="DuplicateButton"/>
                                <Button Content="Delete" x:Name="DeletePlaner"  cal:Bind.Model="{Binding DataContext, RelativeSource={RelativeSource AncestorType=DataGrid}}" cal:Message.Attach="DeleteButton"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Detalji" x:Name="Details" cal:Bind.Model="{Binding DataContext, RelativeSource={RelativeSource AncestorType=DataGrid}}" cal:Message.Attach="DetailsButton"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <ContentControl Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="7" x:Name="ActiveItem" />
        
    </Grid>
</Window>
